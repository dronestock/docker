kind: pipeline
type: docker
name: Docker插件


clone:
  disable: true


trigger:
  event:
    exclude:
      - pull_request


steps:
  - name: 拉代码
    image: hub.daocloud.io/dronestock/git
    pull: always
    settings:
      verbose: true
      debug: true
      ssh_key:
        from_secret: ssh_key_github

  - name: 编译
    image: pangum/drone
    pull: always
    settings:
      output: dockerd
      verbose: true

  - name: 打包Docker到中央库
    image: dronestock/docker
    pull: always
    privileged: true
    settings:
      repository: dronestock/docker
      username: dronestock
      password:
        from_secret: token_docker
